# 阶段 Context：v0.2.0 / A——Context 可执行化

```yaml
stage_context_id: STAGE-V0.2-A-CONTEXT
stage_context_pack_version: 2.7
project: AI Product Engineering Framework
stage: 质量与安全验证
status: active
execution_status: active
current_work_segment: A2 / YouYu v0.1.1 服务端小版本验证
owner: zhidao-studio
human_approver: 项目维护者
started_at: 2026-07-12
project_context_pack_version: 0.2-A.19
baseline_release: v0.1.7
target_release: v0.2.0
reference_project: YouYu
reference_release: v0.1.1
reference_implementation_status: implemented_not_validated
formal_business_validation: not_started
last_verified_at: 2026-07-16
```

> 生命周期映射：A1 将 Context 原则转化为规范和候选模板；A2 使用 YouYu 真实参考工程验证 Context 能否支撑产品、体验、工程规格、受控实现、验证与回写。当前运行状态以 [Framework 项目 Context](../README.md) 为准。

## 1. 进入依据

- v0.1.1 至 v0.1.7 已完成治理、许可、术语、状态收敛、工程修复、业务准备和首个服务端小版本同步；
- Framework 自应用已完成，历史评分 93/100；
- YouYu 工程基础为 `conditional_pass`；
- YouYu 首个业务切片已经完成产品与体验定义；
- 高保真 `v0.1.0-draft.6` 达到人工评审条件但尚未批准；
- 工程差异、既有引用扫描、候选数据模型、接口、错误码和字段映射已经建立；
- 维护者确认 12 项关键参数；
- 维护者通过 YouYu `DEC-004` 授权数据库与服务端基础实现先行；
- YouYu `v0.1.1` 已落地迁移、回滚、OpenAPI 和服务端基础实现；
- [TASK-20260716-018](../任务/TASK-20260716-018_同步YouYuv0.1.1服务端小版本.md)已完成同步。

## 2. 阶段目标

使真实项目能够：

- 维护单一事实源和可恢复 Context；
- 固定产品、体验、工程和任务基线；
- 显式管理人工确认、例外授权和禁止范围；
- 约束数据库、接口、身份、安全和实现边界；
- 区分“已设计”“已实现”“已验证”“已发布”；
- 收集静态、数据库、运行、安全和模拟用户证据；
- 将失败、人工修正、成本和经验回写 Framework；
- 用真实业务判断候选模板和规范是否成熟。

## 3. 工作段

### A1：规范、候选模板与 Framework 自应用

状态：`completed`

已完成四级 Context、独立回写层、事实源与状态、装配与裁剪、冲突与安全、完整性检查、候选模板和 Framework 自应用。

A1 不证明候选模板已经通过真实业务项目验证。

### A2：正式业务参考工程验证

状态：`active`

当前子步骤：**YouYu TASK-011 服务端小版本质量与安全验证**。

#### 已完成

- 产品范围、业务规则、验收断言、用户流程、信息架构和页面说明；
- 高保真候选、主导航和协议双状态；
- 工程基础修复与既有构建证据；
- App 用户与管理员账号域隔离；
- 数据库七字段与 32 位字符串主键规范采用；
- 旧认证、`sys_user`、`BaseEntity`、登录 ID、Controller、Service、Mapper 和 SQL 扫描；
- 12 项关键参数人工确认；
- 四张 App 账号域表迁移、触发器和回滚；
- v0.1.1 OpenAPI 与错误码；
- 字符串身份兼容、Bearer 网关鉴权和精确白名单；
- 验证码、自动注册、协议确认、当前用户、账号信息、昵称和简介服务端基础实现；
- 缺失真实短信、安全密钥、协议、头像和地区能力时失败关闭；
- 维护者服务端先行例外决策；
- TASK-011 验证任务建立。

#### 当前未完成

- MySQL 迁移、触发器、索引、约束和回滚真实执行；
- Maven 测试与打包；
- OpenAPI 自动解析和实现一致性；
- Gateway、App、MySQL、共享会话和内部身份联合运行；
- 验证码错误、过期、使用、频控、并发和协议场景验证；
- 日志与敏感信息检查；
- 真实短信供应商；
- 高保真逐页批准；
- iOS 网络层、Keychain、页面状态和真机验收；
- 安全、网络隔离、采集待审和历史敏感信息关闭；
- 模拟用户验收、运行反馈和 Framework 改进回写。

#### 状态边界

- YouYu 服务端基础实现：`implemented_not_validated`；
- YouYu 正式业务验证：`not_started`；
- 高保真候选不等于批准设计；
- 服务端先行例外不允许 iOS 提前实现；
- Context 模板和数据库规范保持 `candidate`；
- Harness 里程碑 B 尚未开始；
- GitHub Actions 外部阻塞未恢复时，不伪造远程成功证据。

## 4. 本阶段不做

- 不建设通用向量知识库或自动记忆产品；
- 不建设完整 Skill 市场和无人值守 Agent 编排；
- 不将 Framework 自应用、基础构建、HTML 原型或未运行代码替代正式业务验证；
- 不将维护者例外解释为取消全部检查关卡；
- 不宣布候选模板或数据库规范已经稳定；
- 不提前进入 Harness B；
- 不将专有仓库内容发送到未授权系统。

## 5. 必须产物

| 产物 | 当前状态 | 验证要求 |
|---|---|---|
| Framework Context 规范与模板 | `candidate` | 真实业务验证 |
| YouYu 产品与体验 | 已完成 | 高保真仍待批准 |
| YouYu 数据库规范 | 项目已采用 / Framework 候选 | 真实迁移与运行验证 |
| YouYu v0.1.1 迁移与回滚 | 已实现 | MySQL 执行、约束、触发器和回滚 |
| YouYu v0.1.1 OpenAPI | 已实现 | YAML 解析与代码一致性 |
| YouYu 服务端基础实现 | `implemented_not_validated` | Maven、联合运行与安全场景 |
| YouYu iOS 正式实现 | `not_started` | 高保真批准后实施 |
| 完整参考工程验证 | `not_started` | 三层验证、成本复盘和人工批准 |

## 6. 当前任务

| 任务 | 状态 |
|---|---|
| Framework A1 自应用和治理任务 | `completed` |
| YouYu 产品、体验、高保真与工程规格同步 | `completed` |
| YouYu 关键参数确认与 v0.1.1 服务端同步 | `completed` |
| YouYu TASK-011 服务端小版本验证 | `in_progress` |
| YouYu 高保真人工确认 | `in_review` |
| iOS 正式实现任务 | `not_created` |
| Framework 仓库切换 Private | `pending_maintainer_action` |

## 7. 阶段检查关卡

### A2 继续条件

- TASK-011 静态验证无 P0；
- MySQL 迁移与回滚可重复；
- Maven 测试与打包通过；
- Gateway 与 App 受保护接口身份链路有效；
- 验证码和敏感信息安全场景可验证；
- 发现的问题完成回写与修复。

### A2 退出条件

- 高保真由维护者批准；
- iOS 与服务端完成真实端到端路径；
- 静态、数据库、运行、安全和模拟用户验收完成；
- Context 可由新 Agent 独立恢复；
- 修改边界和人工确认点在实际任务中生效；
- 失败、成本、遗漏和人工修正已记录；
- Framework 改进完成回写；
- 人类责任人批准阶段结论。

未满足退出条件前，不升级候选资产成熟度，不进入 Harness B。

## 8. 下一步

```text
TASK-011静态检查
→ Maven测试与打包
→ MySQL迁移与回滚
→ Gateway/App/共享会话联合运行
→ 验证码与敏感信息安全测试
→ 修复问题
→ 高保真人工批准
→ iOS正式实现与Keychain
→ 真机和模拟用户验收
→ Framework回写与A2退出复核
```
