# TASK-20260715-017：同步 YouYu 关键参数确认任务

```yaml
task_id: TASK-20260715-017
status: completed
project: AI Product Engineering Framework
stage: A2 / YouYu 高保真确认与工程规格基线
owner: zhidao-studio
executor: Codex
created_at: 2026-07-15
framework_baseline_commit: a410bda32e714e8ba74892ba6a1b884d1d770993
youyu_source_commit: 1259cc00606e4933608ff9f0c979cf5920ebe761
stable_release_changed: false
framework_maturity_changed: false
```

## 1. 目标

同步 YouYu `TASK-010`：把账号与个人资料工程规格中尚未定稿的业务与安全参数从零散候选文档收敛为一份可由维护者确认的清单。

## 2. 已同步事实

- YouYu 已建立 12 项关键参数候选，涵盖手机号范围、验证码时效与限额、Token、用户编号、账号状态、昵称、简介、头像、地区、协议版本与退出语义；
- 每项仍为 `待确认`，建议候选不等于已批准规则、数据库行为或接口实现；
- 字符串身份链路、头像文件能力、网关公开路径、会话网络边界和历史敏感信息被单独列为实施前依赖，不能通过参数默认值绕过；
- 正式业务实现继续为 `blocked`，正式业务验证继续为 `not_started`。

## 3. Framework 结论

关键参数确认是工程规格进入正式迁移、OpenAPI 与实现任务设计的必要人工环节。Framework 只能沉淀候选、边界和确认记录，不能替代维护者对产品、安全与合规取舍的决定。

## 4. 保持不变的边界

- 不发布新稳定版本，`VERSION` 保持 `0.1.6`；
- 不升级 Context 模板或数据库规范的成熟度；
- 不将高保真候选、参数建议或既有静态扫描描述为正式业务实现或安全验证；
- 不在确认、正式迁移、OpenAPI、实现边界和三层验证缺失时启动业务代码任务。

## 5. 后续检查关卡

1. 维护者完成 YouYu `TASK-010` 的 12 项确认、替代或隔离；
2. 回写 YouYu 业务规则、数据模型、接口、错误码和枚举的唯一事实源；
3. 设计正式数据库迁移、回滚与时间触发器并执行 DB-CHECK；
4. 形成正式 OpenAPI YAML、示例和约定一致性检查；
5. 明确 iOS、服务端、数据库的修改边界及三层验证后，才可建立受控实现任务。
