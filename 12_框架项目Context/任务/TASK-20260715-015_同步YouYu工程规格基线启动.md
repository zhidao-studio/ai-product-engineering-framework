# TASK-20260715-015：同步 YouYu 工程规格基线启动

```yaml
task_id: TASK-20260715-015
status: completed
project: AI Product Engineering Framework
stage: A2 / YouYu 高保真确认与工程规格准备
owner: zhidao-studio
executor: ChatGPT
created_at: 2026-07-15
stable_release_changed: false
framework_maturity_changed: false
```

## 1. 目标

将 YouYu 从高保真候选确认阶段进入工程规格准备阶段的最新事实同步到 Framework，保持参考工程、项目 Context 和成熟度判断一致。

## 2. YouYu 新增事实

- YouYu 当前任务切换为 `TASK-009 建立账号与个人资料工程规格基线`；
- 已确认当前用户名密码登录、独立注册、`Long` 主键、旧基础字段、账号资料混表和手机号普通明文字段与目标业务存在 P0 差异；
- 已建立账号、个人资料、短信验证码和协议确认候选数据模型；
- 已建立发送验证码、统一短信登录、退出、当前用户、资料修改、账号信息、协议和地区候选接口说明；
- 已建立候选错误码；
- 已建立数据库、Java、OpenAPI 和 Swift 候选字段映射；
- 正式 OpenAPI YAML、数据库迁移、关键参数、既有引用扫描、实现任务边界和三层验证仍未完成；
- 正式业务实现继续为 `blocked`，正式业务验证继续为 `not_started`。

## 3. Framework 结论

本次同步证明 Framework 的生命周期可以继续从高保真阶段推进到工程规格设计，并暴露现有基础代码与目标业务规则之间的真实差异；但候选规格尚未经过正式实现和三层验证，因此：

- Context 模板成熟度继续为 `candidate`；
- 数据库基础规范继续为 `candidate`；
- Harness 里程碑 B 不启动；
- 不发布新的稳定版本；
- 不将候选数据模型、接口说明或字段映射写成已实现能力。

## 4. 同步范围

- Framework 项目 Context；
- YouYu 参考工程状态；
- 未发布 v0.2.0 的 A2 进度说明；
- 当前任务索引和风险描述。

## 5. 版本边界

- 根目录 `VERSION` 保持 `0.1.6`；
- 当前目标版本保持 `v0.2.0`；
- 本次只同步参考工程事实，不发布 `v0.1.7`；
- Framework 仓库仍为 Public，专有许可不能代替真正访问控制，仍需维护者手动切换 Private。