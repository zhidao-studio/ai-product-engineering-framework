# TASK-20260723-026：建立任务边界与依赖控制

```yaml
task_id: TASK-20260723-026
task_context_pack_version: 1.1
title: 建立任务边界与依赖控制
status: completed
project: AI Product Engineering Framework
lifecycle_stage: 受控任务执行
risk_level: medium
owner: zhidao-studio
executor: Codex
reviewer: Codex
human_approver: 项目维护者
project_context_pack_version: 0.2-B.3
stage_context_pack_version: 1.0
source_commit: c295d1d65f27613773f3a94a98cce6285c22c353
created_at: 2026-07-23
```

## 1. 目标

将 B1 的风险和阶段控制落实为单次任务可使用的修改边界、依赖授权、候选模板和最小确定性检查。

## 2. 允许修改

- `05_Harness工程/`；
- `08_模板资产/Harness/` 和模板导航；
- `scripts/` 中与任务边界直接相关的最小检查；
- `10_版本演进/Roadmap.md`；
- `12_框架项目Context/`；
- 根目录 README 和 CHANGELOG 的当前状态摘要。

## 3. 禁止修改

- 核心模型、许可和人类最终责任；
- YouYu 业务代码、数据库和运行配置；
- 生产配置、密钥和外部平台；
- 稳定版本和现有资产成熟度；
- Skills、Agents 与 Loop。

## 4. 验收断言

- [x] 任务允许、禁止和高风险范围有唯一权威规范；
- [x] 依赖准入和拒绝条件明确；
- [x] 候选模板能够直接形成任务控制输入；
- [x] 最小检查不依赖模糊语义判断；
- [x] 提供正例、反例和失败停止方式；
- [x] 本地链接、静态和状态一致性检查通过；
- [x] 使用中文提交并推送 `main`；
- [x] 未把候选资产写成已通过 YouYu 验证。

## 5. 验证

- `git diff --check`；
- Markdown 本地链接检查；
- 候选模板正反例检查；
- 最小脚本自测试（如本任务形成脚本）；
- `YOUYU_REPO_DIR=... bash scripts/check-release-state.sh`。

## 6. 风险和停止条件

- 若最小检查需要理解自然语言语义，则先保留人工检查，不编写伪自动化；
- 若需要改变任务 Context Pack 核心字段，则先评估是否需要设计决策；
- 若发现 YouYu 现有做法不可通用，只记录项目实例，不强行纳入 Framework；
- 涉及高风险和严重风险控制放宽时必须停止并请求维护者决定。

## 7. 当前状态

任务已完成：形成修改边界、依赖授权、三份候选模板、机器清单、路径检查脚本和 9 项单元测试。第一次自检发现中文路径转义和本地缓存越界，修复后 11 个真实改动路径通过。结果见 [HARNESS-CHECK-002](../验证/HARNESS-CHECK-002_B2任务边界与依赖控制检查.md)。

未修改 YouYu、生产配置、许可、稳定版本和资产成熟度。最终状态：`completed`。
