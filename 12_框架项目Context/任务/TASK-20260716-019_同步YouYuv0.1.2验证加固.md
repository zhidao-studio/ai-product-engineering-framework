# TASK-20260716-019：同步 YouYu v0.1.2 验证加固

```yaml
task_id: TASK-20260716-019
status: completed
project: AI Product Engineering Framework
stage: 质量与安全验证
owner: zhidao-studio
created_at: 2026-07-16
reference_project: YouYu
reference_release: v0.1.2
framework_release: v0.1.8
formal_reference_validation: not_started
```

## 1. 目标

同步 YouYu v0.1.2 对账号与个人资料服务端切片的静态缺陷修复、测试、数据库断言和联合检查资产，同时保持运行验证、候选资产成熟度和 Harness 里程碑的真实边界。

## 2. 已同步事实

- 修正 Sa-Token 1.39.0 编译包路径；
- 修正验证码错误次数和过期状态的事务回滚问题；
- 将账号、资料和协议记录更新收敛到主键与最小字段；
- 统一 Admin、App、Gateway 的共享 Redis 数据库配置；
- 补齐 App 开发 MySQL 数据源和明确 Mapper 扫描；
- 将成功码、Token 响应、资料边界和未知 JSON 字段与 OpenAPI 对齐；
- 发布 v0.1.2 OpenAPI，删除被替代的 v0.1.1 文件；
- 新增单元测试、数据库结构断言和账号业务切片联合工作流；
- 新增 SERVER-CHECK-002，结论为 `conditional_pass`；
- YouYu TASK-011 转为 `blocked_external_runtime_evidence`。

## 3. 当前证据边界

- GitHub Actions 当前没有 v0.1.2 工作流运行状态；
- 不存在 Maven、MySQL、Redis、Gateway/App 联合运行成功证据；
- YouYu 服务端状态保持 `implemented_not_runtime_validated`；
- YouYu 正式业务验证保持 `not_started`；
- Context 模板和数据库规范保持 `candidate`；
- Harness 里程碑 B 保持 `not_started`。

## 4. Framework 处理

- Framework 稳定版本升级到 `v0.1.8`；
- 项目 Context、参考工程、Roadmap、README 和 CHANGELOG 同步；
- 新增 v0.1.8 同步报告；
- 不将自动检查“已经定义”误写为“已经执行通过”；
- 不因单个参考工程的静态加固提升候选资产成熟度。
